---
description: Never allow Cursor attribution, agent trailers, or Cursor logs in this repo's git history or tree.
alwaysApply: true
---

# No Cursor leak

For **abs-technology/moodle** / this workspace:

- Never add `Co-authored-by: Cursor`, `cursoragent@cursor.com`, `Made-with: Cursor`, or `[Cursor] Checkpoint` to commits, tags, PRs, or Release notes.
- Before commit/push, follow `.cursor/skills/no-cursor-repo-leak/SKILL.md` and ensure `./scripts/git-hooks/install.sh` has been run.
- Never stage agent transcripts, Cursor debug logs, or unrelated `.cursor` junk. Only intentional `.cursor/skills` and `.cursor/rules` may be committed.
